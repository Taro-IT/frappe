{
  "root": "libs/shipping/domain",
  "sourceRoot": "libs/shipping/domain/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/shipping/domain/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/shipping/domain"],
      "options": {
        "jestConfig": "libs/shipping/domain/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
