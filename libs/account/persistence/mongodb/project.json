{
  "root": "libs/account/persistence/mongodb",
  "sourceRoot": "libs/account/persistence/mongodb/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["libs/account/persistence/mongodb/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/libs/account/persistence/mongodb"],
      "options": {
        "jestConfig": "libs/account/persistence/mongodb/jest.config.js",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
