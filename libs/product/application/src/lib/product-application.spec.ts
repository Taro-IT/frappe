import { productApplication } from './product-application';

describe('productApplication', () => {
  it('should work', () => {
    expect(productApplication()).toEqual('product-application');
  });
});
